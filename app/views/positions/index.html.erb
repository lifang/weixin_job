<div class="main_con2">
  <div>
    <div class="toolbar">
      <div class="toolBox">
        <%=link_to  '创建职位', new_company_position_path(@company),class:"white_btn_for_a" %>


        </div>
        <div class="searchTool">
          <div class="searchBox">
            <input type="text" placeholder="搜索职位" />
            <button class="search_btn" onclick="search_position(this,'<%=company_positions_url(@company)%>')"></button>
          </div>
        </div>
      </div>

      <table class="table1">
        <tr>
          <th>职位名称</th>
          <th>职位描述</th>
          <th>操作</th>
        </tr>
        <%@positions.each do |position|%>
          <tr>
            <td><%=position.name%></td>
            <td><%= position.description %></td>
            <td>
              <%=link_to company_position_path(@company,position),method: :delete do%>
                <button class="delete" title="删除" onclick=" return confirm('是否确认删除？') "></button>
              <%end%>
              <%if position.status == 1%>
                <%=link_to release_company_position_path(@company,position) do%>
                  <button class="publish" title="发布职位"></button>
                <%end%>
              <%elsif position.status == 2%>
                 已经发布
              <%end%>
            </td>
          </tr>
        <%end%>
        <%if @positions.blank? %>
          <tr><td colspan="3">暂无招聘哦~~</td></tr>
        <%end%>
      </table>
    </div>
  </div>


